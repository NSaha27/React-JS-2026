# Accordion Component (v1.0)
In this project, I've created an accordion component which is internally managed by react state.

## Features
- It includes an array of topics which will be mapped into different accordion components,
- On toggling each accordion topics, the isOpen state will be changed and a div with related topic description will be displayed or hidden,
- I've used Tailwindcss for styling components
